import { chromium } from "playwright";
import fs from "node:fs";
const port = process.argv[3] ?? "8097";
const browser = await chromium.launch({ executablePath: "/opt/pw-browsers/chromium" });
const page = await browser.newPage({ viewport: { width: 1380, height: 900 } });
const base = `http://localhost:${port}/index.html`;
const out = process.argv[2] ?? "/tmp/claude-0/-home-claude/d2433efc-dba1-5080-a930-43a82e41e69f/scratchpad/shots";
fs.mkdirSync(out, { recursive: true });
const shot = (name) => page.screenshot({ path: `${out}/${name}-${port}.png`, fullPage: true });
page.on("pageerror", (e) => console.log("PAGE ERROR", e.message));
await page.goto(`${base}#/billing`);
await page.waitForTimeout(900);
await shot("billing");
await page.getByRole("tab", { name: "LTCI claims" }).click();
await page.waitForTimeout(500);
await shot("billing-ltci");
await page.getByRole("button", { name: /Open workspace/ }).click();
await page.waitForTimeout(600);
await shot("billing-workspace");
await page.keyboard.press("Escape");
await page.waitForTimeout(300);
await page.getByRole("tab", { name: "Client invoices" }).click();
await page.waitForTimeout(400);
await page.getByRole("button", { name: /^Review invoices/ }).first().click();
await page.waitForTimeout(600);
await shot("billing-review");
await page.keyboard.press("Escape");
await page.waitForTimeout(300);
await page.getByRole("row", { name: /Charles S/ }).first().click();
await page.waitForTimeout(600);
await shot("billing-sheet");
await page.keyboard.press("Escape");
await page.waitForTimeout(300);
await page.getByRole("tab", { name: "Payers" }).click();
await page.waitForTimeout(500);
await shot("billing-payers");
await page.getByRole("row", { name: /Marilyn K/ }).click();
await page.waitForTimeout(600);
await shot("billing-payer-sheet");
await page.keyboard.press("Escape");
await page.waitForTimeout(300);
await page.getByRole("tab", { name: "Profitability" }).click();
await page.waitForTimeout(500);
await shot("billing-profitability");
await browser.close();
